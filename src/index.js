import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'
// import 'bootstrap/dist/css/bootstrap.min.css'; // will import boostrap css // does not work, but works with the react-boiler point

ReactDOM.render(<App />, document.querySelector('#root'));
